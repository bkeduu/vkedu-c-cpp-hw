#pragma once

#include "defaults.h"

short add(skyscraper**, size_t*, string**, size_t*, string**, size_t*, FILE*);
void list(const skyscraper*, const size_t, string*, const size_t, string*, const size_t);
short input_struct(skyscraper*, FILE* in);
